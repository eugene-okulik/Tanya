# 1. Получаем значение арифметической операции.
x = float(input("Введите первое значение (x): "))
y = float(input("Введите второе значение (y): "))

expression = x - y / 5 + x * y

print(f"Результат арифметической операции равен: {expression}")
